import color from '@theme/color'
import styled from 'styled-components/native'

/**
 * Styling code to create a screenContainer
 */
export const ScreenContainer = styled.View`
  flex: 1;
  background-color: ${color.primaryLightest};
`

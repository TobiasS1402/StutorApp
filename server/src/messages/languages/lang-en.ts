import { Language } from "../lang-interface";

export default {
  USER_NOT_FOUND: "User not found",
  DUPLICATE_EMAIL_ERROR: "Email is already registered",
  USER_CREATE_INTERNAL_SERVER_ERROR: "Unknown error when creating a new user",
} as Language;
